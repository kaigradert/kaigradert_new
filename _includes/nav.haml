%header.header{:role => "banner"}
  %h1
    %a{:href => "/", :title => "{{ site.title }}"} {{ site.title }}
  %nav
    {% assign sorted_pages = (site.pages | sort: 'position') %}
    {% for nav in sorted_pages %}
    %a{:class => "{% if nav.url == page.url %}active{% endif %}", :href => "{{ nav.url }}"} {{ nav.title }}
    {% endfor %}