%article{:class => "{% if page.lede == 'yes' %}lede{% endif %}"}

  %h2 {{ page.title | smartypants }}
  {% if content %}
  {{ content }}
  {% endif %}

{% for project in site.work %}
%a{:name => "{{project.slug}}"}
%article.project
  %figure

    {% for image in project.images %}
    %img{:alt => "{{ project.title }}", :src => "{{ image.path }}"}
    {% endfor %}

    {% if project.content %}
    %figcaption {{ project.content }}
    {% endif %}

  {% if project.meta %}
  %dl.meta
    {% for data in project.meta %}
    %div
      %dt {{ project.meta[0] }}
      %dd {{ project.meta[1] }}
    {% endfor %}
  {% endif %}

{% endfor %}